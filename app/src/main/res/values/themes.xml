<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- TEMA BASE CORRIGIDO (AGORA Material COMPONENTS / Material 2) -->
    <style name="Base.Theme.Medvest" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="android:windowBackground">@color/fundo</item>
        <item name="colorPrimary">@color/fundo</item>
        <item name="colorPrimaryVariant">@color/fundo</item>
    </style>

    <style name="EditTextTheme" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="android:background">@drawable/bg_edittext</item>
        <item name="android:padding">12dp</item>
        <item name="android:textSize">14sp</item>
        <item name="android:textColor">#808080</item>
    </style>

    <style name="ButtonTheme" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="android:background">@drawable/edit_botoes</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:textColor">@color/white</item>
        <item name="backgroundTint">@null</item>
    </style>

    <style name="ButtonThemeRed" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="android:background">@drawable/edit_botoes_red</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:textColor">@color/white</item>
        <item name="backgroundTint">@null</item>
    </style>

    <style name="bottomSheetDialogWrapper" parent="Widget.Design.BottomSheet.Modal">
        <item name="android:background">@android:color/transparent</item>
    </style>

    <style name="BottomSheetDialog" parent="Theme.Design.Light.BottomSheetDialog">
        <item name="bottomSheetStyle">@style/bottomSheetDialogWrapper</item>
    </style>

    <!-- TEMA FINAL -->
    <style name="Theme.Medvest" parent="Base.Theme.Medvest"/>

</resources>
