<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Estilo para deixar os CheckBoxes redondos -->
    <style name="RoundCheckBox">
        <item name="android:button">@drawable/ic_checkbox_custom</item>
        <item name="android:paddingStart">8dp</item>
        <item name="cornerSize">50%</item>
    </style>

    <style name="RadioButtonTheme" parent="android:Widget.CompoundButton.RadioButton">
        <item name="android:buttonTint">@color/verde_escuro</item>
        <item name="android:textColor">@color/palavras</item>
        <item name="android:textSize">16sp</item>
    </style>


</resources>
